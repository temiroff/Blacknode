# NVIDIA NIM models are supported natively by LLMAgent via the nim: prefix.
# Example: use a Model picker node with value "nim:meta/llama-3.1-8b-instruct"
#          and connect it to LLMAgent's model port — no separate NIMAgent needed.
